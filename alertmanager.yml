route:
  receiver: 'jenkins-rollback-webhook'

receivers:
- name: 'jenkins-rollback-webhook'
  webhook_configs:
  - url: 'http://35.173.33.252:8080/generic-webhook-trigger/invoke?token=ROLLBACK_9617F8_TOKEN'
    http_config:
      basic_auth:
        username: 'YOUR_JENKINS_USERNAME'
        password: 'YOUR_JENKINS_API_TOKEN'
